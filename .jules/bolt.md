## 2026-05-24 - Disconnecting HttpURLConnection and R Import\n**Learning:** The prompt instructions in memory note that ensuring the correct `R` class (`com.snaptools.shorter.R`) is imported ONLY when actively accessing resources is important. Inexplicably adding the import if it is unused breaks the build. The resource package `com.snaptools.shorter` vs java classes in `com.snaptools.snapurl` causes this to be required. Closing a `BufferedReader` inside a `try-with-resources` block and disconnecting `HttpURLConnection` in a `finally` block improves connection management and reliability.\n**Action:** Verify if `R` is actually needed by looking for `R.` usages before importing or removing the import. Add performance metrics in code comments explicitly as requested by the persona.

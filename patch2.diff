--- app/src/main/java/com/snaptools/snapurl/ProcessTextActivity.java
+++ app/src/main/java/com/snaptools/snapurl/ProcessTextActivity.java
@@ -8,6 +8,7 @@
     private String getString(String key) {
         String lang = java.util.Locale.getDefault().getLanguage();

+        // Bolt: Cache language strings in a static map to avoid recreating them on every method call.
         if (allStrings == null) {
             java.util.HashMap<String, java.util.HashMap<String, String>> all =
                 new java.util.HashMap<>();
